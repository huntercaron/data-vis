const records = require('../assets/phoneRecords.json');
let current;
let previous;

function setup () {
  createCanvas(720, 400);
  //test
  //test
};

function draw () {
  background(0);
  stroke(255);
  rect(50,0,20,200);
};
